# OS AI Assistant Sample

This sample document exercises the index builder. It contains a few sentences so
the chunker can slice text into predictable windows for tests and smoke runs.

- Retrieval: build a JSONL index from raw markdown and JSON.
- Planner: consume the index for scenario tests.
- Executor: patches files inside the sandbox with logged operations.
